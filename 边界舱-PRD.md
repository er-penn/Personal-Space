# 「边界舱」iOS App 项目需求文档 (PRD)

## 一、项目概述

**项目名称**：边界舱 (Boundary Capsule)  
**核心目标**：通过**结构化、异步、低压力**的沟通工具，帮助亲密关系中的双方减少摩擦、保护个人空间、并增进理解。  
**目标用户**：重视个人空间与清晰沟通的情侣，特别是其中一方或双方在传统即时通讯中容易感到压力的人群。  
**设计哲学**："拉取"而非"推送"；提供"确定性"而非"催促"；赋予"控制权"而非施加"要求"。  
**最低支持版本**：**iOS 15.6**

## 二、整体架构与界面布局

App采用经典的**底部标签栏 (Tab Bar)** 导航，分为三个核心空间：

### 1. 「我的空间」 (My Space) - 默认首页
**功能**：**管理自我**。所有以"我"为中心的功能集中于此。

**布局**：
- **顶部状态区**：显示用户自己的当前状态（大号🟢/🟡/🔴图标）和「专注模式」开关
- **功能卡片区**：从上至下排列「能量预规划」、「焦虑平复指南」、「知行合一」、「我的动态」、「随手记」等功能入口
- **悬浮按钮 (FAB)**：右下角"+"按钮。点击后弹出菜单，包含：【随手记】、【发起邀请】、【送达通知】、【赠送心意】、【分享碎片】、【发布瞬间】。**这是所有对外发起动作的总入口**

### 2. 「我们的空间」 (Our Space)
**功能**：**处理协作与共享**。一个**只读的、需要用户主动拉取信息**的仪表盘。

**布局**：
- **顶部状态区**：显示伴侣的当前状态
- **信息列表区**：卡片列表形式展示"待我处理"和"我发起的"事项
- **伴侣信息区**：折叠卡片形式展示"情绪报告"、"近日安排"、"TA的分享"（碎片收件箱入口）、"TA的动态"
- **共同记录区**：放置「Maybe清单」和「成长花园」

### 3. 「个人中心」 (Profile)
**功能**：**进行设置**。

**布局**：列表形式。包含：账号设置、关系管理、通知配置、工具盒（内含「冷静空间」）

## 三、功能清单与细节

### 「我的空间」 Tab - (管理自我)

#### 1. 能量信号 (Energy Signal)
- **描述**：每日设置基础情绪能量状态（🟢/🟡/🔴）
- **UI/逻辑**：大图标展示在首页顶部。状态变更后，伴侣端同步更新（仅展示）

#### 2. 专注模式 (Focus Mode)
- **描述**：临时开启免打扰时段
- **UI/逻辑**：首页顶部开关（🌙）。开启后全App静音，并向伴侣端发送提示

#### 3. 能量快充 (Energy Boost)
- **描述**：在低能量日临时开启沟通窗口
- **UI/逻辑**：在状态为🟡/🔴时，在「能量信号」区域显示快捷入口。开启后状态临时变🟢

#### 4. 能量预规划 (Energy Planner)
- **描述**：为未来时间段预约低能量状态
- **UI/逻辑**：功能卡片，点击进入日历视图进行规划

#### 5. 焦虑平复指南 (Anxiety Soothing Guide)
- **描述**：私密的自我安抚工具集
- **UI/逻辑**：功能卡片。在App顶部状态栏有常驻图标，通过柔和动画提示（如呼吸光晕），用户需主动点击进入

#### 6. 知行合一 (Action & Knowledge)
- **描述**：私密的个人目标与习惯管理
- **UI/逻辑**：功能卡片。点击进入私人目标列表/打卡页

#### 7. 我的动态 (My Moments)
- **描述**：查看自己发布的所有"瞬间"
- **UI/逻辑**：功能卡片。点击进入列表页，管理本人发布的内容

#### 8. 随手记 (Quick Note)
- **描述**：快速记录想法、观察、灵感，支持文字、语音、图片混合记录，并可进行多维度拆解分析
- **UI/逻辑**：
  - **入口1**：功能卡片（位于"我的动态"下方，"焦虑平复指南"上方）。显示最近N条记录预览，点击进入随手记列表页
  - **入口2**：FAB菜单中新增"随手记"选项，点击后直接进入创建随手记页面
- **记录方式**：支持文字、语音、图片三种方式混合记录
- **标题生成**：
  - 有文字内容：自动取文字内容的前10个字作为标题
  - 无文字内容：按时间戳自动生成标题（格式：随手记20251120001）
- **拆解功能**：记录后可进行三个维度的拆解分析
  - **洞察**：对人、制度、系统的理解和观察
  - **痛点**：记录中可能隐含的需求和问题
  - **方案**：满足需求的解决方案，以及将其标准化并推广的可能性
- **列表展示**：
  - 以标题为主，文字内容为辅（只显示一行，超出用"..."截断）
  - 按创建时间或最近编辑时间（两者中最新的一个）倒序排列
  - 支持筛选：全部、未拆解、有"洞察"、有"痛点"、有"方案"
- **详情编辑**：点击记录后可查看详情，支持修改内容、标题和拆解

#### 9. 心情快照 (Mood Snapshot)
- **描述**：私密的每日轻量情绪记录
- **UI/逻辑**：在首页提供快速记录入口（滑动条）

### FAB 发起的次级页面

#### 10. 随手记创建页 (New Quick Note) - 微信聊天式界面
- **触发**：点击FAB -> 【随手记】
- **UI/逻辑**：
  - **全屏显示**：非弹窗，全屏页面
  - **微信聊天式界面**：
    - 消息列表区域：显示所有消息，右对齐，微信绿色背景（`#95EC69`）
    - 底部输入栏：文字输入框 + 语音切换按钮（旋转90度wifi信号图标 `📶`）+ 加号菜单
  - **消息类型**：文字、语音、图片、角标消息（洞察/痛点/方案）
  - **标题**：默认显示时间戳（格式：`yyMMddHHmmss`，如 `251120194125`），可通过右上角`⋯`修改
  - **发送逻辑**：
    - 文字消息：使用输入法发送按钮发送
    - 语音消息：长按录音，松手发送（根据时长显示不同长度，100pt-200pt）
    - 图片消息：选择后自动发送
    - 角标消息：通过"+"菜单添加角标，可继续编辑，使用输入法发送按钮发送
  - **引用功能**：长按消息可引用，引用消息自动继承被引用消息的角标
  - **返回逻辑**：
    - 未发送任何内容：直接返回，销毁新建的随手记
    - 已发送内容：返回时自动保存

#### 11. 协作邀请创建页 (New Collaboration Invitation)
- **触发**：点击FAB -> 【发起邀请】
- **UI/逻辑**：表单页。包含标题、内容、可选时间等字段。**提交时校验：无法发起24小时内的邀请**

#### 12. 安心闭环创建页 (New Peaceful Closure)
- **触发**：点击FAB -> 【送达通知】
- **UI/逻辑**：表单页。包含物品、地点、预计时间等字段。极度简化，去情感化文案

#### 13. 心意盒创建页 (New Gift Box)
- **触发**：点击FAB -> 【赠送心意】
- **UI/逻辑**：表单页。必填字段包括物品、心意准备时间、建议地点；选填备注；有效期设置（默认7天）。
- **物品输入**：支持自定义输入，提供快速选择标签（礼物、下午茶、正餐、日常用品）
- **心意准备时间**：限制对方最早可接受时间（当前时间+准备时间）
- **有效期**：可开启/关闭，开启后选择截止时间，到期自动从接收方待处理中移除

#### 14. 碎片发送页 (New Fragment)
- **触发**：点击FAB -> 【分享碎片】
- **UI/逻辑**：简易编辑器。可添加图片、链接或短文字。每日最多发送2个

#### 15. 瞬间发布页 (New Moment)
- **触发**：点击FAB -> 【发布瞬间】
- **UI/逻辑**：编辑器。可添加图片和文案。**有清晰提示："文案将默认对伴侣隐藏3天"**

### 「我们的空间」 Tab - (处理协作与共享)

#### 16. 情绪图谱 (Emotion Map)
- **描述**：查看伴侣共享的周期性情绪评估摘要
- **UI/逻辑**：通过**推送触发评估**。评估完成后，结果以卡片形式静默出现在"伴侣信息区"

#### 17. 协作邀请列表页 (Collaboration Invitation List)
- **描述**：处理"协作邀请"
- **UI/逻辑**：在"待我处理"和"我发起的"列表中查看。对方可选：【好呀】、【商量下呗】、【以后看】

- **【好呀】响应**：系统自动在"待我处理"列表中创建"待办事项"提醒。该待办事项无需用户交互，仅在活动时间（开始时间+持续时间）结束后自动消失

- **【商量下呗】响应**：用户可选择以下两种处理方式：
  1. **修改后推送确认**：可修改活动起始时间、地点、持续时间、活动内容，其中活动时间和活动内容可提供多个选项供对方选择，加快协商效率。协商修改次数无限制。
  2. **直接微信商量**：不修改任何内容，直接点击"微信商量"，对方收到微信商量通知，同时系统创建待办事项（仅可手动关闭，不会自动消失）

- **【以后看】响应**：对方收到"以后看"通知，同时将活动地点和活动内容自动存入Maybe清单

#### 18. 安心闭环列表页 (Peaceful Closure List)
- **描述**：处理"安心闭环"
- **UI/逻辑**：在"待我处理"和"我发起的"列表中查看。接收方点击【已知悉】即完成闭环

#### 19. 心意盒列表页 (Gift Box List)
- **描述**：处理"心意盒"
- **UI/逻辑**：
  - **接收方响应页**：显示物品、建议地点、备注。提供两种响应方式：
    - "好滴收下啦🥰"：需填写接受时间范围（起始时间≥当前时间+准备时间）和接受地点
    - "不太想要😅"：直接拒绝，无需填写额外信息
  - **发起方管理页**：按最新活动时间倒序显示所有心意盒。每个订单显示状态标签（待确认、已接受、不想要、已过期、已撤回）
  - **操作权限**：
    - 待确认状态：只能"撤回"
    - 其他状态：可"再次编辑发送"
    - 已接受状态：额外提供"发起安心确认"选项，自动跳转到安心确认页面并预填物品信息
  - **有效期处理**：开启有效期的心意盒到期自动从接收方待处理中移除，发起方仍可重新激活

#### 20. 碎片收件箱页 (Fragment Inbox)
- **描述**：异步批量处理分享信息
- **UI/逻辑**：入口在"伴侣信息区"。**仅当用户自身状态为🟢时，入口才亮起可点击**

#### 21. 瞬间浏览页 (Moments Feed)
- **描述**：查看伴侣发布的动态
- **UI/逻辑**：在"伴侣信息区"展示。**文案默认隐藏3天**，3天后自动公开。**用户状态为🔴时，隐藏此板块**

#### 22. Maybe清单 (Maybe List)
- **描述**：无压力的共同愿望灵感记录
- **UI/逻辑**：位于"我们的空间"的**共同记录区**。双方可随时编辑。**无任何通知**
- **扩展功能**：
  - 当用户选择【以后看】时，活动地点和内容自动存入Maybe清单
  - 新建活动邀请时可选择"从Maybe清单导入"，复用之前的活动创意

#### 23. 成长花园 (Growth Garden)
- **描述**：关系积极互动的抽象可视化
- **UI/逻辑**：位于"我们的空间"的**共同记录区**。采用"共养植物"等抽象可视化，**无数字**

### 「个人中心」 Tab - (设置与管理)

#### 24. 关系管理 (Relationship Management)
- **描述**：核心设置功能。邀请或解除伴侣关系
- **UI/逻辑**：在「个人中心」以列表项形式存在。通过生成邀请链接实现绑定

#### 25. 冷静空间 (Calm Space)
- **描述**：为外部冲突提供结构化暂停工具
- **UI/逻辑**：位于「个人中心」的"工具盒"内。提供生成"暂停对话"文案等功能

## 四、核心技术栈与框架 (iOS 15.6+)

- **语言**: **Swift**
- **UI框架**: **SwiftUI** (主体) + **UIKit** (必要时兼容)
- **架构模式**: **MVVM**
- **数据持久化 (本地)**: **Core Data**
- **网络与同步**: **CloudKit** (首选)
- **推送通知**: **APNs via CloudKit**
- **依赖管理**: **Swift Package Manager (SPM)**

## 五、全局规则

1. **通知规则**: **默认关闭所有通知**。用户可手动在「个人中心」-「通知设置」中为特定功能开启
2. **状态联动规则**: "我们的空间"内容的**可访问性与用户自身的当前能量状态绑定**
3. **无红点规则**: 禁止使用未读数字角标（红点）
4. **数据共享规则**: 所有数据默认私有。共享数据遵循"最小化"和"用户可控"原则

## 六、技术实现要点

### 数据模型设计
- 用户状态管理（能量信号、专注模式）
- 关系绑定与邀请机制
- 各种内容类型的数据结构（邀请、闭环、心意盒、碎片、瞬间、随手记等）
- 本地缓存与CloudKit同步策略

### 状态管理
- 全局状态管理（用户状态、关系状态）
- 各Tab页面的状态管理
- 数据同步状态管理

### 用户体验设计
- 无压力交互设计
- 状态驱动的UI显示逻辑
- 异步加载与缓存策略
- 错误处理与用户反馈

## 七、开发里程碑

### Phase 1: 基础架构
- [ ] 项目初始化与基础架构搭建
- [ ] 底部Tab导航实现
- [ ] 基础数据模型设计
- [ ] Core Data + CloudKit集成

### Phase 2: 我的空间核心功能
- [ ] 能量信号系统
- [ ] 专注模式实现
- [ ] 基础功能卡片
- [ ] FAB菜单与次级页面

### Phase 3: 我们的空间
- [ ] 伴侣状态显示
- [ ] 信息列表管理
- [ ] 各种内容类型处理
- [ ] 共同记录功能

### Phase 4: 个人中心与设置
- [ ] 关系管理功能
- [ ] 通知设置
- [ ] 工具盒功能

### Phase 5: 优化与测试
- [ ] 性能优化
- [ ] 用户体验优化
- [ ] 全面测试
- [ ] App Store准备

---

**文档版本**: v1.3  
**创建日期**: 2025年1月  
**最后更新**: 2025年11月20日

---

## 八、随手记功能详细设计

### 8.1 功能概述

随手记是一个快速记录和深度思考的工具，允许用户以多种方式（文字、语音、图片）记录想法，并通过三个维度的拆解分析，将日常观察转化为有价值的洞察。

### 8.2 数据模型

```swift
struct QuickNote: Identifiable, Codable {
    let id: UUID
    var title: String                    // 标题（默认：时间戳格式 yyMMddHHmmss，可手动修改）
    var messages: [NoteMessage]          // 消息列表
    var createdAt: Date                  // 创建时间
    var updatedAt: Date                  // 最近编辑时间
    
    // 整理内容（从角标消息中整理得出）
    var insightSummary: String?          // 洞察整理内容
    var painPointSummary: String?         // 痛点整理内容
    var solutionSummary: String?          // 方案整理内容
    
    // 生成默认标题（时间戳格式）
    static func generateDefaultTitle(from date: Date = Date()) -> String {
        let formatter = DateFormatter()
        formatter.dateFormat = "yyMMddHHmmss"
        return formatter.string(from: date)
    }
    
    // 计算属性
    var hasContent: Bool {
        return !messages.isEmpty
    }
    
    var latestUpdateTime: Date {
        return max(createdAt, updatedAt)
    }
    
    // 检查是否有拆解整理
    var hasInsight: Bool {
        return insightSummary != nil && !insightSummary!.isEmpty ||
               messages.contains { $0.badge == .insight }
    }
    
    var hasPainPoint: Bool {
        return painPointSummary != nil && !painPointSummary!.isEmpty ||
               messages.contains { $0.badge == .painPoint }
    }
    
    var hasSolution: Bool {
        return solutionSummary != nil && !solutionSummary!.isEmpty ||
               messages.contains { $0.badge == .solution }
    }
    
    var isAnalyzed: Bool {
        return hasInsight || hasPainPoint || hasSolution
    }
}

struct NoteMessage: Identifiable, Codable {
    let id: UUID
    var type: MessageType                // 文字/语音/图片
    var content: String?                 // 文字内容
    var audioPath: String?               // 语音文件路径
    var audioDuration: TimeInterval?     // 语音时长（用于显示不同长度）
    var imagePath: String?               // 图片文件路径
    var badge: BadgeType?                // 角标类型（洞察/痛点/方案）
    var referencedMessageId: UUID?        // 引用的消息ID
    var referencedMessagePreview: String? // 引用消息预览（前50字）
    var createdAt: Date
    
    // 检查是否有内容
    var hasContent: Bool {
        return content != nil && !content!.isEmpty ||
               audioPath != nil ||
               imagePath != nil
    }
    
    // 计算语音消息气泡宽度（根据时长）
    var audioBubbleWidth: CGFloat {
        guard let duration = audioDuration else { return 100 }
        // 最小宽度100pt，最大宽度200pt，根据时长线性增长
        let minWidth: CGFloat = 100
        let maxWidth: CGFloat = 200
        let maxDuration: TimeInterval = 60 // 60秒为最大时长
        let width = minWidth + (maxWidth - minWidth) * CGFloat(min(duration, maxDuration) / maxDuration)
        return width
    }
}

enum MessageType: String, Codable {
    case text = "文字"
    case audio = "语音"
    case image = "图片"
}

enum BadgeType: String, Codable {
    case insight = "洞察"
    case painPoint = "痛点"
    case solution = "方案"
}
```

### 8.3 页面结构

#### 8.3.1 我的空间 - 随手记卡片
- **位置**：位于"我的动态"卡片下方，"焦虑平复指南"卡片上方
- **显示内容**：
  - 标题："随手记"
  - 预览：显示最近N条记录的标题（最多3条）
  - 统计：总记录数
- **交互**：点击卡片进入随手记列表页

#### 8.3.2 随手记列表页 (QuickNoteListView)
- **顶部导航**：标题"随手记"，返回按钮，右上角"+"按钮（新建随手记）
- **筛选标签栏**：
  - [全部] - 显示所有记录
  - [未拆解] - 显示没有任何拆解整理的记录（没有任何角标消息或整理内容）
  - [有洞察] - 显示有"洞察"角标消息或整理内容的记录
  - [有痛点] - 显示有"痛点"角标消息或整理内容的记录
  - [有方案] - 显示有"方案"角标消息或整理内容的记录
- **列表区域**：
  - 每条记录显示为卡片（类似微信主界面的聊天历史）
  - **卡片布局**（两行结构）：
    - **第一行**：随手记标题（主）
      - 字号：较大（如 `headline` 或 `title3`）
      - 颜色：主文字颜色（如 `AppTheme.Colors.text`）
      - 加粗显示
    - **第二行**：最近一次消息预览（辅）
      - 字号：较小（如 `body` 或 `subheadline`）
      - 颜色：次要文字颜色（如 `AppTheme.Colors.textSecondary`）
      - 消息预览格式：
        - **文字消息**：显示文字内容（一行，超出用"..."截断）
        - **语音消息**：显示 `[语音]15''` 格式（15表示秒数）
        - **图片消息**：显示 `[图片]`
        - **角标消息**：显示消息内容 + 角标标记（如 `[洞察]`）
  - 排序：按最近更新时间倒序（最新消息的随手记在最上面）
- **交互**：
  - 点击筛选标签：过滤列表
  - 点击记录卡片：进入聊天窗界面（与FAB菜单打开的页面是同一个）
  - 点击右上角"+"：创建新的随手记（进入聊天窗界面）

#### 8.3.3 创建/编辑随手记页 (QuickNoteChatView) - 微信聊天式界面
- **页面样式**：全屏显示（非弹窗），类似微信聊天窗口
- **顶部导航栏**：
  - 左侧：返回按钮 `←`
    - 未发送任何内容：直接返回，销毁新建的随手记
    - 已发送内容：返回时自动保存
  - 中间：时间戳标题（格式：`yyMMddHHmmss`，如 `251120194125`）
    - 新建时：自动生成当前时间戳
    - 编辑时：显示随手记的标题（时间戳）
  - 右侧：三个点 `⋯`
    - 点击弹出菜单：
      - **编辑标题**：修改随手记标题
      - **洞察**：进入洞察整理页（如果该随手记有洞察角标的消息）
      - **痛点**：进入痛点整理页（如果该随手记有痛点角标的消息）
      - **方案**：进入方案整理页（如果该随手记有方案角标的消息）
    - 菜单项显示逻辑：
      - 如果该随手记没有对应角标的消息，则不显示对应菜单项
      - 如果该随手记有整理内容，菜单项可以显示已整理标记（可选）
- **消息列表区域**：
  - **消息对齐**：所有消息靠右对齐（类似微信自己发送的消息）
  - **消息类型**：
    - **文字消息**：
      - 显示文字内容
      - 背景色：微信绿色（`#95EC69`）
      - 圆角气泡样式（圆角12pt，内边距12pt上下，16pt左右）
      - 最大宽度：屏幕宽度的75%
    - **语音消息**：
      - 显示波形图标 + 时长（如 `🎤 00:15`）
      - 背景色：微信绿色（`#95EC69`）
      - 根据时长显示不同长度（最短100pt，最长200pt，根据时长线性增长）
      - 圆角气泡样式
      - **可带角标**：语音消息可以带三种角标中任意一种（`[洞察]` / `[痛点]` / `[方案]`），但只能带一种
      - 角标显示在消息底部
    - **图片消息**：
      - 显示图片缩略图
      - 圆角样式
      - **可带角标**：图片消息可以带三种角标中任意一种（`[洞察]` / `[痛点]` / `[方案]`），但只能带一种
      - 角标显示在消息底部
    - **文字角标消息**：
      - 文字 + 角标（`[洞察]` / `[痛点]` / `[方案]`）
      - 角标显示在消息底部
      - 背景色：微信绿色
    - **角标颜色**：
      - 洞察：黄色 `#FFD700`
      - 痛点：橙色 `#FF8C00`
      - 方案：绿色 `#32CD32`
  - **消息特性**：
    - 发送后不可修改
    - 长按消息弹出菜单：`引用`、`删除`
    - 引用消息显示引用标记和引用内容预览
    - **引用继承角标**：如果被引用消息有角标，当前消息自动继承该角标
- **底部输入栏**（固定底部）：
  - **左侧**：语音输入切换按钮（旋转90度的wifi信号图标 `📶`）
    - 点击切换：文字输入框 ↔ 语音按钮
    - 语音模式：长按录音，松手发送
  - **中间**：文字输入框
    - 占位符："输入文字..."
    - 支持多行输入
    - **发送方式**：使用输入法自带的发送按钮发送
  - **右侧**：加号按钮 `+`
    - 点击展开菜单：
      - **照片**：打开相册选择图片，选择后自动发送（可选择角标）
      - **拍摄**：调用相机拍摄，拍摄后自动发送（可选择角标）
      - **洞察**：给当前输入框内容加上"洞察"角标（不发送，可继续编辑）
      - **痛点**：给当前输入框内容加上"痛点"角标（不发送，可继续编辑）
      - **方案**：给当前输入框内容加上"方案"角标（不发送，可继续编辑）
    - **角标选择逻辑**：
      - 发送语音或图片消息时，可以选择添加角标（三种角标中任意一种，但只能选一种）
      - 选择角标后，消息发送时自动带上角标
- **消息发送逻辑**：
  - **文字消息**：输入文字 → 点击输入法发送按钮 → 发送
  - **角标消息**：输入文字 → 点击"+"选择角标类型 → 角标添加到输入框（可继续编辑）→ 点击输入法发送按钮 → 发送（带角标）
  - **语音消息**：切换到语音模式 → 长按录音 → 松手发送（可选择角标）
  - **图片消息**：点击"+" → 选择"照片/拍摄" → 选择/拍摄后 → 可选择角标 → 自动发送（带角标）
  - **引用消息**：长按消息 → 选择"引用" → 输入新内容 → 点击输入法发送按钮发送（如果被引用消息有角标，自动继承）
- **编辑标题功能**：
  - 点击右上角 `⋯` → 选择"编辑标题" → 弹出编辑标题的弹窗
  - 输入新标题 → 确认 → 更新标题
- **角标整理功能**：
  - **入口1**：点击聊天窗中消息的角标 → 进入对应的整理页面（弹窗形式）
  - **入口2**：点击右上角 `⋯` → 选择"洞察"/"痛点"/"方案" → 进入对应的整理页面
  - **整理页面结构**：
    - **消息展示区**：
      - 按时间顺序排列（最早的在上面，最新的在最下面）
      - 不按日期分组，连续显示
      - 只显示当前聊天中带有该角标的消息
      - 每条消息显示完整内容：
        - 文字消息：显示文字内容
        - 语音消息：显示 `[语音]15''` 格式（15表示秒数）
        - 图片消息：显示 `[图片]`
    - **底部编辑框**：
      - 始终处于可编辑状态
      - 可以输入和编辑整理内容
      - 占位符：如"整理你的洞察..."（根据角标类型变化）
    - **保存功能**：
      - 右上角"保存"按钮
      - 点击后保存编辑框内容到 `QuickNote` 模型的对应字段（`insightSummary` / `painPointSummary` / `solutionSummary`）

#### 8.3.4 角标整理页 (BadgeSummaryView)
- **页面类型**：三种独立的整理页面（弹窗形式）
  - 洞察整理页
  - 痛点整理页
  - 方案整理页
- **顶部导航**：
  - 左侧：返回按钮 `←`
  - 中间：标题（如"洞察整理"）
  - 右侧："保存"按钮
- **消息展示区**：
  - **排序**：按时间顺序排列（最早的在上面，最新的在最下面）
  - **不分组**：不按日期分组，连续显示
  - **筛选**：只显示当前聊天中带有该角标的消息
  - **显示内容**：每条消息显示完整内容
    - 文字消息：显示文字内容
    - 语音消息：显示 `[语音]15''` 格式（15表示秒数）
    - 图片消息：显示 `[图片]`
- **底部编辑框**：
  - 始终处于可编辑状态
  - 可以输入和编辑整理内容
  - 占位符：如"整理你的洞察..."（根据角标类型变化）
- **保存功能**：
  - 点击右上角"保存"按钮
  - 保存编辑框内容到 `QuickNote` 模型的对应字段
    - 洞察整理页 → `insightSummary`
    - 痛点整理页 → `painPointSummary`
    - 方案整理页 → `solutionSummary`
- **入口**：
  - **入口1**：点击聊天窗中消息的角标 → 进入对应的整理页面
  - **入口2**：点击右上角 `⋯` → 选择"洞察"/"痛点"/"方案" → 进入对应的整理页面

### 8.4 功能入口

#### 入口1：我的空间卡片
- **位置**：我的空间页面，"我的动态"下方，"焦虑平复指南"上方
- **跳转**：点击卡片 → 随手记列表页

#### 入口2：FAB菜单
- **位置**：我的空间页面右下角FAB按钮
- **菜单项**：新增"随手记"选项
- **跳转**：点击"随手记" → 全屏打开创建随手记页（微信聊天式界面）

### 8.5 数据持久化

- **存储方式**：本地存储（UserDefaults 或 Core Data）
- **数据同步**：暂不考虑后端同步（后续可扩展）
- **文件存储**：
  - 语音文件：存储在本地Documents目录
  - 图片文件：存储在本地Documents目录
  - 文件路径保存在QuickNote模型中

### 8.6 技术实现要点

- **语音录制**：使用AVFoundation框架
- **图片选择**：使用UIImagePickerController或PHPickerViewController
- **图片展示**：支持多图滑动查看
- **语音播放**：使用AVAudioPlayer
- **标题自动生成**：默认使用时间戳格式（`yyMMddHHmmss`），用户可通过右上角`⋯`修改
- **时间戳生成**：使用DateFormatter格式化当前时间（格式：`yyMMddHHmmss`，如 `251120194125`）
- **消息气泡样式**：
  - 右对齐布局
  - 微信绿色背景（`#95EC69`）
  - 圆角12pt，内边距12pt（上下），16pt（左右）
  - 最大宽度：屏幕宽度的75%
- **语音消息长度**：根据时长动态计算气泡宽度（100pt-200pt）
- **引用功能**：长按消息可引用，引用消息自动继承被引用消息的角标
- **角标功能**：
  - 文字消息：通过"+"菜单添加角标，角标仅标记，不影响发送，可继续编辑后通过输入法发送按钮发送
  - 语音消息：发送时可以选择添加角标（三种角标中任意一种，但只能选一种）
  - 图片消息：发送时可以选择添加角标（三种角标中任意一种，但只能选一种）
- **角标整理功能**：
  - 点击消息角标或右上角菜单可进入整理页面
  - 整理页面显示所有该角标的消息（按时间顺序，最新的在最下面）
  - 底部编辑框可编辑整理内容，保存到对应的整理字段
