# 前后端对接方案 - 能量模块

## 一、模块概述

本文档描述以下四个模块的前后端对接方案：
1. **我的空间顶部状态栏** - 显示当前能量状态、临时状态、预规划状态、伴侣状态
2. **能量记录模块** - 显示7:00-23:59的能量记录，按分钟级显示
3. **能量预规划模块** - 显示预规划的能量状态
4. **我们的空间顶部状态栏** - 显示伴侣的能量状态和能量记录

## 二、数据需求分析

### 2.1 我的空间顶部状态栏

#### 前端需要的数据：

| 数据项 | 类型 | 说明 |
|--------|------|------|
| `currentBaseEnergyLevel` | String | 当前基础能量状态：`"🟢"` / `"🟡"` / `"🔴"` / `"⚪"` |
| `displayEnergyLevel` | String | 显示的能量状态（优先级：临时状态 > 预规划状态 > 基础状态） |
| `isTemporaryStateActive` | Boolean | 是否处于临时状态 |
| `currentTemporaryStateType` | String | 临时状态类型：`"fastCharge"` / `"lowPower"` |
| `temporaryStateRemainingTime` | Integer | 临时状态剩余时间（分钟） |
| `isPlannedStateActive` | Boolean | 是否处于预规划状态 |
| `currentPlannedStateLevel` | String | 当前预规划状态的能量等级 |
| `plannedStateRemainingTime` | Integer | 预规划状态剩余时间（分钟） |
| `partnerEnergyLevel` | String | 伴侣能量状态：`"🟢"` / `"🟡"` / `"🔴"` / `"⚪"` |

#### 后端需要提供的数据结构：

```json
{
  "current_status": {
    "base_energy_level": "⚪",
    "display_energy_level": "🟢",
    "temporary_state": {
      "is_active": false,
      "type": null,
      "remaining_minutes": 0
    },
    "planned_state": {
      "is_active": false,
      "level": null,
      "remaining_minutes": 0
    }
  },
  "partner_status": {
    "energy_level": "🟡"
  }
}
```

### 2.2 能量记录模块

#### 前端需要的数据：

| 数据项 | 类型 | 说明 |
|--------|------|------|
| `baseEnergyPlans` | Array | 基础状态记录数组（按日期分组） |
| `plannedEnergyPlans` | Array | 预规划状态数组（按日期分组） |
| `temporaryStatePlans` | Array | 临时状态数组（按日期分组） |

每个EnergyPlan包含：
- `id`: UUID
- `date`: Date (ISO 8601格式)
- `energyLevel`: String (`"🟢"` / `"🟡"` / `"🔴"` / `"⚪"`)
- `timeSlots`: Array<TimeSlot>
  - `startHour`: Integer (0-23)
  - `startMinute`: Integer (0-59)
  - `endHour`: Integer (0-23)
  - `endMinute`: Integer (0-59)

#### 后端需要提供的数据结构：

```json
{
  "records": {
    "base": [
      {
        "id": "uuid",
        "date": "2025-01-27",
        "energy_level": "🟢",
        "time_slots": [
          {
            "start_hour": 7,
            "start_minute": 0,
            "end_hour": 10,
            "end_minute": 30
          }
        ]
      }
    ],
    "planned": [
      {
        "id": "uuid",
        "date": "2025-01-27",
        "energy_level": "🟡",
        "time_slots": [
          {
            "start_hour": 14,
            "start_minute": 0,
            "end_hour": 18,
            "end_minute": 0
          }
        ]
      }
    ],
    "temporary": [
      {
        "id": "uuid",
        "date": "2025-01-27",
        "energy_level": "🟢",
        "temporary_type": "fastCharge",
        "time_slots": [
          {
            "start_hour": 9,
            "start_minute": 0,
            "end_hour": 11,
            "end_minute": 0
          }
        ]
      }
    ]
  },
  "summary": {
    "high_minutes": 120,
    "medium_minutes": 180,
    "low_minutes": 60,
    "unplanned_minutes": 240
  }
}
```

### 2.3 能量预规划模块

#### 前端需要的数据：

| 数据项 | 类型 | 说明 |
|--------|------|------|
| `plannedEnergyPlans` | Array | 预规划状态数组（按日期分组） |

每个EnergyPlan包含：
- `id`: UUID
- `date`: Date (ISO 8601格式)
- `energyLevel`: String (`"🟢"` / `"🟡"` / `"🔴"` / `"⚪"`)
- `timeSlots`: Array<TimeSlot>

#### 后端需要提供的数据结构：

```json
{
  "plans": [
    {
      "id": "uuid",
      "date": "2025-01-27",
      "energy_level": "🟡",
      "time_slots": [
        {
          "start_hour": 14,
          "start_minute": 0,
          "end_hour": 18,
          "end_minute": 0
        }
      ],
      "created_at": "2025-01-27T10:00:00Z"
    }
  ]
}
```

### 2.4 我们的空间顶部状态栏

#### 前端需要的数据：

| 数据项 | 类型 | 说明 |
|--------|------|------|
| `partnerEnergyLevel` | String | 伴侣能量状态：`"🟢"` / `"🟡"` / `"🔴"` / `"⚪"` |
| `partnerEnergyRecords` | Array | 伴侣能量记录（7:00-23:59，按分钟级） |

伴侣能量记录结构同能量记录模块，但只包含基础状态记录。

#### 后端需要提供的数据结构：

```json
{
  "partner_status": {
    "energy_level": "🟡",
    "records": {
      "base": [
        {
          "id": "uuid",
          "date": "2025-01-27",
          "energy_level": "🟡",
          "time_slots": [
            {
              "start_hour": 7,
              "start_minute": 0,
              "end_hour": 10,
              "end_minute": 30
            }
          ]
        }
      ]
    }
  }
}
```

## 三、API接口设计

### 3.1 获取当前能量状态

**接口**: `GET /api/v1/energy/current-status/`

**响应**:
```json
{
  "current_status": {
    "base_energy_level": "⚪",
    "display_energy_level": "🟢",
    "temporary_state": {
      "is_active": false,
      "type": null,
      "remaining_minutes": 0
    },
    "planned_state": {
      "is_active": false,
      "level": null,
      "remaining_minutes": 0
    }
  },
  "partner_status": {
    "energy_level": "🟡"
  }
}
```

### 3.2 获取能量记录

**接口**: `GET /api/v1/energy/records/`

**查询参数**:
- `date` (可选): 日期，格式 `YYYY-MM-DD`，默认今天
- `type` (可选): 记录类型，`base` / `planned` / `temporary`，默认全部

**响应**:
```json
{
  "records": {
    "base": [...],
    "planned": [...],
    "temporary": [...]
  },
  "summary": {
    "high_minutes": 120,
    "medium_minutes": 180,
    "low_minutes": 60,
    "unplanned_minutes": 240
  }
}
```

### 3.3 获取能量预规划

**接口**: `GET /api/v1/energy/plans/`

**查询参数**:
- `date` (可选): 日期，格式 `YYYY-MM-DD`，默认今天

**响应**:
```json
{
  "plans": [...]
}
```

### 3.4 获取伴侣能量状态

**接口**: `GET /api/v1/energy/partner-status/`

**响应**:
```json
{
  "partner_status": {
    "energy_level": "🟡",
    "records": {
      "base": [...]
    }
  }
}
```

### 3.5 更新当前能量状态

**接口**: `PUT /api/v1/energy/current-status/`

**请求体**:
```json
{
  "energy_level": "🟢"
}
```

**响应**:
```json
{
  "energy_level": "🟢",
  "updated_at": "2025-01-27T10:00:00Z"
}
```

### 3.6 创建临时状态

**接口**: `POST /api/v1/energy/temporary-state/`

**请求体**:
```json
{
  "type": "fastCharge",
  "duration_minutes": 120
}
```

**响应**:
```json
{
  "id": "uuid",
  "type": "fastCharge",
  "start_time": "2025-01-27T10:00:00Z",
  "end_time": "2025-01-27T12:00:00Z",
  "remaining_minutes": 120
}
```

### 3.7 结束临时状态

**接口**: `DELETE /api/v1/energy/temporary-state/`

**响应**:
```json
{
  "message": "临时状态已结束"
}
```

### 3.8 创建能量预规划

**接口**: `POST /api/v1/energy/plans/`

**请求体**:
```json
{
  "date": "2025-01-27",
  "energy_level": "🟡",
  "time_slots": [
    {
      "start_hour": 14,
      "start_minute": 0,
      "end_hour": 18,
      "end_minute": 0
    }
  ]
}
```

**响应**:
```json
{
  "id": "uuid",
  "date": "2025-01-27",
  "energy_level": "🟡",
  "time_slots": [...],
  "created_at": "2025-01-27T10:00:00Z"
}
```

## 四、数据库结构调整

### 4.1 EnergyRecord模型调整

当前模型已基本符合需求，但需要确保：
1. `time_slots` 字段存储为JSON数组，格式：
   ```json
   [
     {
       "start_hour": 7,
       "start_minute": 0,
       "end_hour": 10,
       "end_minute": 30
     }
   ]
   ```
2. `energy_level` 字段使用emoji值：`"🟢"` / `"🟡"` / `"🔴"` / `"⚪"`
3. `record_type` 字段：`"base"` / `"planned"` / `"temporary"`

### 4.2 User模型调整

需要在User模型中添加：
- `current_energy_level`: CharField，存储当前基础能量状态
- `last_energy_update`: DateTimeField，最后更新时间

### 4.3 新增字段说明

如果User模型中没有以下字段，需要添加：
```python
current_energy_level = models.CharField(
    max_length=10,
    choices=EnergyRecord.EnergyLevel.choices,
    default=EnergyRecord.EnergyLevel.UNPLANNED,
    verbose_name='当前能量状态'
)
```

## 五、后端实现要点

### 5.1 当前状态计算逻辑

后端需要实现以下优先级逻辑：
1. **临时状态优先级最高**：如果当前时间在临时状态时间段内，返回临时状态
2. **预规划状态优先级次之**：如果当前时间在预规划状态时间段内，返回预规划状态
3. **基础状态**：返回用户当前的基础能量状态

### 5.2 分钟级记录处理

后端需要实现每分钟自动追加基础状态时间段的逻辑：
- 使用Celery定时任务，每分钟执行一次
- 检查用户当前基础能量状态
- 追加到对应日期的基础状态记录中

### 5.3 伴侣状态同步

当用户能量状态变更时，需要：
1. 通过WebSocket通知伴侣
2. 更新伴侣的缓存状态
3. 记录到数据库

## 六、前端实现要点

### 6.1 数据获取时机

1. **应用启动时**：获取当前状态和今日记录
2. **进入"我的空间"时**：刷新当前状态和记录
3. **进入"我们的空间"时**：刷新伴侣状态
4. **每分钟更新**：通过定时器更新倒计时和当前状态

### 6.2 数据缓存策略

1. **当前状态**：实时获取，不缓存
2. **历史记录**：按日期缓存，当天数据实时更新
3. **预规划数据**：按日期缓存，创建/更新时刷新

### 6.3 状态同步

1. **本地状态变更**：立即更新UI，然后同步到后端
2. **后端状态变更**：通过WebSocket接收，更新本地状态
3. **冲突处理**：以后端数据为准

## 七、实施步骤

### 阶段一：数据库和模型调整（1-2天）
1. 调整EnergyRecord模型，确保time_slots格式正确
2. 在User模型中添加current_energy_level字段
3. 创建数据库迁移

### 阶段二：后端API实现（3-5天）
1. 实现获取当前状态API
2. 实现获取能量记录API
3. 实现获取预规划API
4. 实现获取伴侣状态API
5. 实现更新状态API
6. 实现创建临时状态API
7. 实现创建预规划API

### 阶段三：前端对接（3-5天）
1. 在APIService中添加能量相关方法
2. 修改UserState，从后端获取数据
3. 修改PartnerState，从后端获取数据
4. 更新MySpaceView，使用后端数据
5. 更新EnergyProgressView，使用后端数据
6. 更新EnergyPlanningView，使用后端数据
7. 更新OurSpaceView，使用后端数据

### 阶段四：测试和优化（2-3天）
1. 功能测试
2. 性能优化
3. 错误处理
4. 边界情况处理

## 八、注意事项

1. **时区处理**：所有时间相关数据统一使用UTC时间，前端显示时转换为本地时间
2. **数据格式**：确保前后端数据格式一致，特别是时间格式和能量状态枚举值
3. **性能优化**：能量记录数据量大，需要考虑分页和缓存策略
4. **实时性**：当前状态需要实时更新，考虑使用WebSocket推送
5. **数据一致性**：确保基础状态、预规划状态、临时状态的数据一致性

## 九、数据格式对照表

### 能量状态枚举

| 前端值 | 后端值 | 说明 |
|--------|--------|------|
| `EnergyLevel.high` | `"🟢"` | 满血复活 |
| `EnergyLevel.medium` | `"🟡"` | 血条还行 |
| `EnergyLevel.low` | `"🔴"` | 血槽空了 |
| `EnergyLevel.unplanned` | `"⚪"` | 待规划 |

### 记录类型枚举

| 前端值 | 后端值 | 说明 |
|--------|--------|------|
| `base` | `"base"` | 基础状态 |
| `planned` | `"planned"` | 预规划状态 |
| `temporary` | `"temporary"` | 临时状态 |

### 临时状态类型枚举

| 前端值 | 后端值 | 说明 |
|--------|--------|------|
| `TemporaryStateType.fastCharge` | `"fastCharge"` | 快充模式 |
| `TemporaryStateType.lowPower` | `"lowPower"` | 低电量模式 |

